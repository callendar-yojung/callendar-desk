name: Tauri Build & Upload

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: windows
          - os: macos-14
            platform: macos
          - os: ubuntu-22.04
            platform: linux

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Install system dependencies (Ubuntu)
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev

      - name: Install dependencies
        run: npm ci

      - name: Build Tauri app
        run: npm run tauri:build
        env:
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}

      - name: Get version from tag
        id: version
        shell: bash
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Find and prepare artifacts
        id: artifacts
        shell: bash
        run: |
          VERSION=${{ steps.version.outputs.VERSION }}
          PLATFORM=${{ matrix.platform }}
          ZIP_NAME="desktop-calendar-${VERSION}-${PLATFORM}.zip"

          echo "=== Debug: Searching for bundle directories ==="

          # Find bundle directory dynamically - it could be in different locations
          # depending on the platform and architecture
          BUNDLE_DIR=$(find src-tauri/target -type d -name "bundle" 2>/dev/null | grep -E "release/bundle$" | head -1)

          if [[ -z "$BUNDLE_DIR" ]]; then
            echo "Error: No bundle directory found"
            echo "=== Listing src-tauri/target structure ==="
            find src-tauri/target -type d -maxdepth 4 2>/dev/null || echo "No target directory"
            exit 1
          fi

          echo "=== Found bundle directory: $BUNDLE_DIR ==="
          ls -la "$BUNDLE_DIR" || echo "Cannot list bundle dir"

          if [[ "$PLATFORM" == "windows" ]]; then
            # Windows: find .msi or .exe (NSIS) file
            ARTIFACT=$(find "$BUNDLE_DIR" -name "*.msi" -type f 2>/dev/null | head -1)
            if [[ -z "$ARTIFACT" ]]; then
              ARTIFACT=$(find "$BUNDLE_DIR" -name "*.exe" -type f 2>/dev/null | head -1)
            fi
          elif [[ "$PLATFORM" == "macos" ]]; then
            # macOS: find .dmg file
            ARTIFACT=$(find "$BUNDLE_DIR" -name "*.dmg" -type f 2>/dev/null | head -1)
          else
            # Linux: find .deb or .AppImage file
            ARTIFACT=$(find "$BUNDLE_DIR" -name "*.deb" -type f 2>/dev/null | head -1)
            if [[ -z "$ARTIFACT" ]]; then
              ARTIFACT=$(find "$BUNDLE_DIR" -name "*.AppImage" -type f 2>/dev/null | head -1)
            fi
          fi

          echo "=== Found artifact: $ARTIFACT ==="

          if [[ -z "$ARTIFACT" || ! -f "$ARTIFACT" ]]; then
            echo "Error: No artifact found for platform $PLATFORM"
            echo "=== Listing all files in bundle directory ==="
            find "$BUNDLE_DIR" -type f 2>/dev/null || echo "No files in bundle"
            exit 1
          fi

          # Get artifact directory and filename
          ARTIFACT_DIR=$(dirname "$ARTIFACT")
          ARTIFACT_NAME=$(basename "$ARTIFACT")

          echo "Artifact dir: $ARTIFACT_DIR"
          echo "Artifact name: $ARTIFACT_NAME"

          # Create zip in workspace root
          cd "$ARTIFACT_DIR"

          if [[ "$PLATFORM" == "windows" ]]; then
            7z a "$GITHUB_WORKSPACE/$ZIP_NAME" "$ARTIFACT_NAME"
          else
            zip -j "$GITHUB_WORKSPACE/$ZIP_NAME" "$ARTIFACT_NAME"
          fi

          cd "$GITHUB_WORKSPACE"

          # Verify zip exists
          if [[ ! -f "$ZIP_NAME" ]]; then
            echo "Error: ZIP file not created"
            ls -la
            exit 1
          fi

          echo "=== ZIP created: $ZIP_NAME ==="
          ls -la "$ZIP_NAME"

          # Calculate file size
          if [[ "$PLATFORM" == "windows" ]]; then
            FILE_SIZE=$(powershell -command "(Get-Item '$ZIP_NAME').Length")
          elif [[ "$PLATFORM" == "macos" ]]; then
            FILE_SIZE=$(stat -f%z "$ZIP_NAME")
          else
            FILE_SIZE=$(stat -c%s "$ZIP_NAME")
          fi

          # Calculate checksum
          if [[ "$PLATFORM" == "windows" ]]; then
            CHECKSUM=$(powershell -command "(Get-FileHash '$ZIP_NAME' -Algorithm SHA256).Hash.ToLower()")
          else
            CHECKSUM=$(shasum -a 256 "$ZIP_NAME" | cut -d ' ' -f 1)
          fi

          echo "ZIP_NAME=$ZIP_NAME" >> $GITHUB_OUTPUT
          echo "ZIP_PATH=$GITHUB_WORKSPACE/$ZIP_NAME" >> $GITHUB_OUTPUT
          echo "FILE_SIZE=$FILE_SIZE" >> $GITHUB_OUTPUT
          echo "CHECKSUM=$CHECKSUM" >> $GITHUB_OUTPUT

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Upload to S3
        id: s3_upload
        shell: bash
        run: |
          VERSION=${{ steps.version.outputs.VERSION }}
          PLATFORM=${{ matrix.platform }}
          ZIP_NAME=${{ steps.artifacts.outputs.ZIP_NAME }}

          S3_PATH="s3://${{ secrets.AWS_S3_BUCKET }}/releases/v${VERSION}/${PLATFORM}/${ZIP_NAME}"
          aws s3 cp "$ZIP_NAME" "$S3_PATH"

          S3_URL="https://${{ secrets.AWS_S3_BUCKET }}.s3.${{ secrets.AWS_REGION }}.amazonaws.com/releases/v${VERSION}/${PLATFORM}/${ZIP_NAME}"
          echo "S3_URL=$S3_URL" >> $GITHUB_OUTPUT

      - name: Save release info to database
        shell: bash
        continue-on-error: true
        run: |
          VERSION=${{ steps.version.outputs.VERSION }}
          PLATFORM=${{ matrix.platform }}
          ZIP_NAME=${{ steps.artifacts.outputs.ZIP_NAME }}
          FILE_SIZE=${{ steps.artifacts.outputs.FILE_SIZE }}
          CHECKSUM=${{ steps.artifacts.outputs.CHECKSUM }}
          S3_URL=${{ steps.s3_upload.outputs.S3_URL }}

          # Escape commit message for JSON
          RELEASE_NOTES=$(echo "${{ github.event.head_commit.message }}" | head -1 | sed 's/"/\\"/g' | tr -d '\n\r')

          curl -X POST "${{ secrets.API_BASE_URL }}/api/releases" \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer ${{ secrets.API_SECRET_KEY }}" \
            --fail \
            -d "{
              \"version\": \"${VERSION}\",
              \"platform\": \"${PLATFORM}\",
              \"fileName\": \"${ZIP_NAME}\",
              \"downloadUrl\": \"${S3_URL}\",
              \"fileSize\": ${FILE_SIZE},
              \"checksum\": \"${CHECKSUM}\",
              \"releaseNotes\": \"${RELEASE_NOTES}\",
              \"isPrerelease\": false
            }"

      - name: Upload artifact for release
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.platform }}-build
          path: ${{ steps.artifacts.outputs.ZIP_NAME }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Get version
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: List artifacts
        run: find artifacts -type f

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*.zip
          tag_name: v${{ steps.version.outputs.VERSION }}
          name: Release v${{ steps.version.outputs.VERSION }}
          generate_release_notes: true
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
